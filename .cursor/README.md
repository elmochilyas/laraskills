# Cursor IDE Configuration

Place `rules.mdc` in your project's `.cursor/` directory to activate Laravel 13 rules.

## How to Use

1. Copy `.cursor/rules.mdc` to your Laravel 13 project's `.cursor/rules.mdc`
2. Cursor will automatically apply these rules when editing PHP files
