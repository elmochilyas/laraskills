| Metadata | |
|---|---|
| KU ID | ku-01 |
| Subdomain | vector-similarity-search |
| Topic | Vector Embeddings Concept |
| Source | Academic / Industry |
| Maturity | Mature |

## Overview

Vector embeddings convert text (or images, audio, etc.) into numerical vector representations that capture semantic meaning. Similar concepts produce similar vectors, enabling semantic search. Embeddings are generated by neural network models (transformers) and stored in vector databases or indexes for similarity search.

## Core Concepts

- **Embedding Vector**: Fixed-size numerical array (e.g., 1536 dimensions for OpenAI text-embedding-3-small)
- **Semantic Proximity**: Similar vectors ≈ similar meaning
- **Dimensionality**: Higher dimensions capture more nuance but require more storage
- **Normalization**: Unit vectors ensure cosine similarity = dot product
- **Embedding Models**: OpenAI, Cohere, BGE, E5, FastEmbed
- **Use Cases**: Semantic search, clustering, classification, recommendation

## When To Use

- Semantic search beyond keyword matching
- RAG pipelines for document retrieval
- Multi-language search
- Recommendation systems (similar items)
- Content clustering and categorization

## When NOT To Use

- Exact keyword matching required (SKUs, codes, proper nouns)
- Latency or cost of embedding generation is prohibitive
- Small datasets where keyword search is sufficient

## Best Practices

1. **Use normalized embeddings**: Ensures consistent distance computation.
2. **Choose appropriate dimensionality**: 1536 (OpenAI small) for most cases.
3. **Cache embeddings**: Never re-embed the same text.
4. **Batch embedding calls**: More efficient for bulk processing.
5. **Match distance metric to model**: Use recommended metric for your embedding model.

## Related Topics

- K067 (Embedding generation strategies)
- K041 (pgvector extension)
- K053 (Qdrant FastEmbed)

## AI Agent Notes

- Vector embeddings enable semantic understanding beyond keyword matching
- OpenAI text-embedding-3-small is the most common starting point
- For agents: start with API embeddings, cache aggressively

## Verification

- [ ] Understand embedding generation
- [ ] Embedding dimensionality chosen
- [ ] Embeddings normalized
- [ ] Distance metric matches model
- [ ] Embedding caching implemented
