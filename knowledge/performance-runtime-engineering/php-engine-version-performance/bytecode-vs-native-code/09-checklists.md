# Metadata
**Domain:** performance-runtime-engineering
**Subdomain:** PHP Engine & Version Performance
**Knowledge Unit:** Bytecode vs Native Code â€” OpCache Stores Opcodes, JIT Compiles to Machine Code
**Generated:** 2026-06-03

# Quick Checklist (10-20 derived items)
- [ ] **Enable OpCache first**: Provides 2-4x throughput vs no opcode caching. Single highest-ROI change.
- [ ] **Add JIT for CPU-bound workloads**: JIT provides 61-95% additional gain for CPU-bound tasks but only 0-5% for I/O-bound web apps.
- [ ] **Size JIT buffer appropriately**: Default 128MB is sufficient for most workloads. CPU-heavy apps may benefit from 256MB.
- [ ] **Understand the two-level cache**: OpCache caches bytecode across requests (shared memory). JIT caches native code within a process (per-request buffer).
- [ ] OpCache enabled in production
- [ ] JIT enabled (opcache.jit=1255 for tracing mode)
- [ ] JIT buffer sized appropriately for workload (64-256MB)
- [ ] OpCache hit rate > 99%
- [ ] Understanding of bytecode vs native code distinction demonstrated
- [ ] JIT benefit quantified as percentage improvement in throughput or latency
- [ ] Baseline and post-JIT benchmarks documented with environment details
- [ ] JIT mode selection justified by workload profile
- [ ] Buffer sizing verified adequate for the compiled code
- [ ] PHP execution time measured as percentage of wall time
- [ ] Baseline benchmark with JIT disabled completed
- [ ] Benchmark with tracing JIT (1254) completed
- [ ] Benchmark with function JIT (1205) completed if applicable
- [ ] JIT buffer utilization verified (free >20%)
- [ ] Results compared: identify if JIT provides >5% improvement
- [ ] **PHP version lifecycle**: Always run a supported PHP version (currently 8.1+). Each minor version brings 5-15% performance improvement. Upgrade within 3 months of release.

# Architecture Checklist (responsibilities, layer, boundaries, deps + items from 04/05/07)
- [ ] Define clear architectural responsibilities and boundaries for this component
- [ ] **Shared-nothing architecture** (PHP-FPM): Each request is isolated in a separate OS process. Maximizes fault isolation at the cost of per-request bootstrap overhead. Best for multi-tenant hosting where isolation is prioritized.
- [ ] **Memory-resident architecture** (Octane/Swoole): Boot once, handle many. Reduces latency by 60-90% for framework-heavy applications but introduces state management complexity. Best for dedicated API servers with controlled code deployments.
- [ ] **Event-driven coroutines** (Swoole/FrankenPHP): Single process handles many concurrent requests via coroutine switching. Memory efficiency is high but requires non-blocking I/O for all operations.
- [ ] **Pipeline**: Source -> Lexer -> Parser -> Compiler -> Opcodes -> (JIT) -> Native code -> Execution
- [ ] **OpCache interception**: On warm requests, OpCache serves pre-compiled opcodes from shared memory, skipping lex/parse/compile phases entirely.
- [ ] **JIT interception**: At opcode dispatch level, JIT redirects hot traces to compiled native code paths, skipping the VM dispatch loop.
- [ ] Document and follow through on architectural decision: OpCache vs JIT investment priority
- [ ] Document and follow through on architectural decision: Whether JIT native code compilation is worth the RAM
- [ ] Ensure architecture aligns with core concept: **Bytecode/opcodes**: Platform-independent intermediate representation (~200 instruction types). Each opcode is ~12 bytes. Stored in OpCache shared memory.
- [ ] Ensure architecture aligns with core concept: **Native code**: CPU-specific machine instructions. Generated by JIT compiler via DynASM. Stored in JIT buffer (separate from OpCache).
- [ ] Ensure architecture aligns with core concept: **VM dispatch overhead**: Each opcode requires handler function call, operand decode, and result write-back â€” ~5-15 CPU cycles per opcode.
- [ ] Ensure architecture aligns with core concept: **JIT eliminates dispatch**: Native code has no opcode fetch/decode overhead; operations flow directly via registers.

# Implementation Checklist (classes, naming, DI, error handling + items from 04/05/06)
- [ ] Use appropriate class structure and naming conventions
- [ ] Apply dependency injection where applicable
- [ ] Implement proper error handling and type safety
- [ ] **Enable OpCache first**: Provides 2-4x throughput vs no opcode caching. Single highest-ROI change.
- [ ] **Add JIT for CPU-bound workloads**: JIT provides 61-95% additional gain for CPU-bound tasks but only 0-5% for I/O-bound web apps.
- [ ] **Size JIT buffer appropriately**: Default 128MB is sufficient for most workloads. CPU-heavy apps may benefit from 256MB.
- [ ] **Understand the two-level cache**: OpCache caches bytecode across requests (shared memory). JIT caches native code within a process (per-request buffer).
- [ ] Profile the target code path to determine PHP execution time as percentage of wall time
- [ ] If PHP execution time <30% of wall time, JIT benefit will be minimal (0-5%) â€” proceed only if CPU-bound
- [ ] Classify the code: loop-heavy (tracing JIT benefits), function-call-heavy (function JIT benefits), or mixed
- [ ] Disable JIT (opcache.jit=0) and run a benchmark to establish baseline
- [ ] Enable tracing JIT (opcache.jit=1254) and re-run the same benchmark
- [ ] Compare results: if throughput improves >5%, JIT is beneficial for this workload
- [ ] Optionally test function JIT (opcache.jit=1205) for function-heavy workloads
- [ ] Monitor JIT buffer utilization: if free space <20%, increase jit_buffer_size
- [ ] If no significant improvement, keep JIT enabled anyway (harmless overhead 0-2%) but do not prioritize further JIT tuning

# Performance Checklist (from 04/06)
- [ ] Shared-nothing (FPM)
- [ ] Memory-resident (Octane)
- [ ] JIT compilation
- [ ] OpCache

# Security Checklist (from 04/06 - only if relevant)
- [ ] JIT buffer is writable and executable memory â€” a JIT bug could theoretically be exploited
- [ ] JIT is disabled when running PHP in debug mode or with certain opcode caches
- [ ] OpCache shared memory is isolated per-user in multi-tenant environments

# Reliability Checklist (from 04/05/06)
- [ ] **Deadlock**: PHP-FPM workers deadlocked on shared resource (file lock, database connection pool). Symptom: active workers plateau at max_children, no requests complete. Mitigation: Set request_terminate_timeout to kill stuck workers.
- [ ] **Memory exhaustion**: PHP worker exceeds memory_limit. Symptom: PHP Fatal error "Allowed memory size exhausted". Mitigation: Increase limit or optimize memory usage. Root cause analysis via memory profiler.
- [ ] **File descriptor exhaustion**: OpCache or PHP worker runs out of file descriptors. Symptom: "Too many open files" errors. Mitigation: Increase ulimit -n in systemd service file.
- [ ] **PHP version lifecycle**: Always run a supported PHP version (currently 8.1+). Each minor version brings 5-15% performance improvement. Upgrade within 3 months of release.
- [ ] **Configuration audit**: Check memory_limit, max_execution_time, max_input_vars, ealpath_cache_size are tuned for your application. Defaults are conservative.
- [ ] **Error handling**: display_errors=Off, log_errors=On, error_reporting=E_ALL. Never show errors to users.

# Testing Checklist (from 04/06)
- [ ] OpCache enabled in production
- [ ] JIT enabled (opcache.jit=1255 for tracing mode)
- [ ] JIT buffer sized appropriately for workload (64-256MB)
- [ ] OpCache hit rate > 99%
- [ ] Understanding of bytecode vs native code distinction demonstrated
- [ ] JIT benefit verified against workload profile (CPU-bound vs I/O-bound)
- [ ] JIT benefit quantified as percentage improvement in throughput or latency
- [ ] Baseline and post-JIT benchmarks documented with environment details
- [ ] JIT mode selection justified by workload profile
- [ ] Buffer sizing verified adequate for the compiled code
- [ ] PHP execution time measured as percentage of wall time
- [ ] Baseline benchmark with JIT disabled completed
- [ ] Benchmark with tracing JIT (1254) completed
- [ ] Benchmark with function JIT (1205) completed if applicable
- [ ] JIT buffer utilization verified (free >20%)
- [ ] Results compared: identify if JIT provides >5% improvement

# Maintainability Checklist
- [ ] Document key decisions and implementation rationale
- [ ] Follow consistent code style and naming conventions
- [ ] Keep components focused and single-purpose
- [ ] Ensure clear API boundaries and contracts
- [ ] **Enable OpCache first**: Provides 2-4x throughput vs no opcode caching. Single highest-ROI change.
- [ ] **Add JIT for CPU-bound workloads**: JIT provides 61-95% additional gain for CPU-bound tasks but only 0-5% for I/O-bound web apps.
- [ ] **Size JIT buffer appropriately**: Default 128MB is sufficient for most workloads. CPU-heavy apps may benefit from 256MB.
- [ ] **Understand the two-level cache**: OpCache caches bytecode across requests (shared memory). JIT caches native code within a process (per-request buffer).

# Anti-Pattern Prevention Checklist (per anti-pattern in 08, per common mistake in 04)
- [ ] Avoid: Mistake
- [ ] Avoid: Assuming JIT replaces OpCache
- [ ] Avoid: Not sizing JIT buffer
- [ ] Avoid: Expecting JIT to fix I/O-bound apps
- [ ] Avoid: Disabling OpCache in production
- [ ] Avoid anti-pattern: **Treating bytecode and native code as alternatives**: They are complementary layers. OpCache caches bytecode; JIT compiles hot bytecode to native code. Enable both.
- [ ] Avoid anti-pattern: **Over-provisioning JIT buffer**: 256MB+ for I/O-bound lightweight apps wastes RAM. Size based on workload profile.
- [ ] Avoid anti-pattern: **Disabling OpCache for "freshness"**: Use opcache_reset() for deployments instead of disabling OpCache entirely.
- [ ] Guard against anti-pattern: Expecting JIT to Solve All Performance Problems
- [ ] Guard against anti-pattern: Confusing Bytecode Caching (OpCache) with JIT Compilation
- [ ] Guard against anti-pattern: Measuring JIT Performance with Cold Cache Benchmarks
- [ ] Guard against anti-pattern: Enabling JIT Without CPU-bound Workload Verification
- [ ] Guard against anti-pattern: Ignoring JIT Compilation Overhead in Short-Lived Processes
- [ ] Workload profiled to determine CPU vs I/O ratio
- [ ] JIT enabled only if CPU time exceeds 50% of total request time

# Production Readiness Checklist
- [ ] Configure monitoring and alerting
- [ ] Set up logging with appropriate verbosity levels
- [ ] Document operational runbooks
- [ ] **PHP version lifecycle**: Always run a supported PHP version (currently 8.1+). Each minor version brings 5-15% performance improvement. Upgrade within 3 months of release.
- [ ] **Configuration audit**: Check memory_limit, max_execution_time, max_input_vars, ealpath_cache_size are tuned for your application. Defaults are conservative.
- [ ] **Error handling**: display_errors=Off, log_errors=On, error_reporting=E_ALL. Never show errors to users.

# Final Approval Checklist
- [ ] All Quick Checklist items verified
- [ ] Architecture decisions reviewed and approved
- [ ] Implementation follows best practices
- [ ] Performance requirements met
- [ ] Production readiness validated
- [ ] Tests pass and coverage meets threshold

# Related Knowledge/Rules/Skills/Trees/Anti-Patterns
**Core Concepts:** **Bytecode/opcodes**: Platform-independent intermediate representation (~200 instruction types). Each opcode is ~12 bytes. Stored in OpCache shared memory., **Native code**: CPU-specific machine instructions. Generated by JIT compiler via DynASM. Stored in JIT buffer (separate from OpCache)., **VM dispatch overhead**: Each opcode requires handler function call, operand decode, and result write-back â€” ~5-15 CPU cycles per opcode., **JIT eliminates dispatch**: Native code has no opcode fetch/decode overhead; operations flow directly via registers.
**Rules:**
- General: Size JIT Buffer According to Workload Profile
**Skills:** JIT Configuration for Production, Workload Benefit Assessment, Benchmark Design and Execution
**Decision Trees:** OpCache vs JIT investment priority, Whether JIT native code compilation is worth the RAM
**Anti-Patterns:** Expecting JIT to Solve All Performance Problems, Confusing Bytecode Caching (OpCache) with JIT Compilation, Measuring JIT Performance with Cold Cache Benchmarks, Enabling JIT Without CPU-bound Workload Verification, Ignoring JIT Compilation Overhead in Short-Lived Processes
**Related Topics:** PHP Execution Lifecycle, OpCache Purpose and Mechanics, JIT Concepts and Terminology, OpCache Memory Sizing, JIT Buffer Sizing Guidelines

