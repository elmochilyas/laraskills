# Decomposition: Bytecode Vs Native Code

## Topic Overview
PHP operates at two compiled-code levels: **bytecode** (Zend opcodes, stored by OpCache) and **native machine code** (x86-64/ARM64 instructions, generated by JIT). Bytecode is interpreted by the Zend VM; native code runs directly on the CPU. OpCache eliminates re-compilation to bytecode. JIT eliminates VM dispatch overhead for hot code paths.

## Decomposition Strategy
This Knowledge Unit is atomic -- it covers a single, well-bounded concept with independent decisions, tradeoffs, and architecture guidance. No further decomposition is needed.

## Proposed Folder Structure
```
php-engine-performance/bytecode-vs-native-code/
  02-knowledge-unit.md
  03-decomposition.md
```

## Knowledge Unit Inventory

### Bytecode Vs Native Code
- **Purpose:** PHP operates at two compiled-code levels: **bytecode** (Zend opcodes, stored by OpCache) and **native machine code** (x86-64/ARM64 instructions, generated by JIT). Bytecode is interpreted by the Zend VM; native code runs directly on the CPU. OpCache eliminates re-compilation to bytecode. JIT eliminates VM dispatch overhead for hot code paths.
- **Difficulty:** Foundation
- **Dependencies:
  - --

## Dependency Graph
**Depends on:**
  - --

**Depended by:**
  (Referenced by other KUs at the subdomain level)

## Boundary Analysis
**In scope:**
  - Pipeline model
  - Bottleneck-first approach

**Out of scope:**
  (Related topics are covered in other Knowledge Units within the same subdomain)

## Future Expansion Opportunities
None identified -- the topic is stable and well-bounded at this granularity.
---

## Success Criteria

This decomposition is complete when:

✓ No Knowledge Unit is overloaded

✓ No major concept is missing

✓ Boundaries are clear

✓ Future phases can operate on individual units

✓ The structure can scale without reorganization